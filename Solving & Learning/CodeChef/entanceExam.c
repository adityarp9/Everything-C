#include <stdio.h>

int n;
scanf("%d", &n);
int main(void)
{
    /*
    printf("Enter the number of students:");
    printf("%d", n);

    typedef struct student
    {
        int sNo, marks[n], totalScore;
    }*/
    return 0;
}
